# 测试
pytest>=8.2.0
pytest-cov>=5.0.0

# 质量工具
pre-commit>=3.7.0
ruff>=0.5.0
black>=24.4.0
isort>=5.13.0
mypy>=1.10.0

# 文档（可选）
mkdocs>=1.6.0
mkdocs-material>=9.5.0

# CI 额外驱动（仅用于 migrate-check 的 Postgres）
psycopg[binary]>=3.1.18

# 可选：SQLite CLI/异步，若本地用得到再保留
aiosqlite>=0.20.0
