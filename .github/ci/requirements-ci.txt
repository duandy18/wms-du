# 运行 pytest 所需的最小依赖 + DB 驱动（CI 内显式安装，避免缺包）
pytest>=8.2
pytest-asyncio>=0.23

alembic>=1.13
sqlalchemy>=2.0
psycopg[binary]>=3.1
asyncpg>=0.29

# HTTP 层测试（ASGITransport / AsyncClient）
fastapi>=0.111
uvicorn>=0.30
httpx>=0.27
pydantic-settings>=2.4
