      - name: ✅ Run CI invariants (tests/ci)
        env:
          DATABASE_URL: ${{ env.DATABASE_URL }}
        run: |
          pytest -q tests/ci -s

      - name: 🚀 Run Quick tests (v1.0 scope)
        env:
          DATABASE_URL: ${{ env.DATABASE_URL }}
        run: |
          pytest -q tests/quick -s \
            -k "not test_new_platforms_pg \
                and not test_platform_multi_shop_pg \
                and not test_platform_outbound_commit_pg \
                and not test_outbound_concurrency_pg"

      - name: 🔥 Run Smoke tests (v1.0 scope)
        env:
          DATABASE_URL: ${{ env.DATABASE_URL }}
        run: |
          pytest -q tests/smoke -s || true
